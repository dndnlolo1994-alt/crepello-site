Dark form input with mono label and caramel focus ring.

```jsx
<Input label="Email" type="email" placeholder="you@table.co" />
<Input label="City" hint="Where should we open next?" />
```

Border lifts to `--border-accent` and surface brightens on focus. Use the mono `label` for the editorial feel.
