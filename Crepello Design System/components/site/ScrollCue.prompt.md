Subtle hero scroll cue — a vertical tracked label over a slowly drifting caramel line.

```jsx
<ScrollCue label="Scroll" />
```

Place bottom-center of the hero. The drift loop stills under `prefers-reduced-motion`. Decorative only — keep it out of the tab order.
